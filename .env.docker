# Docker build-time environment — copy this to .env before running docker-compose
# Replace <EC2-PUBLIC-IP> with your actual EC2 instance public IP or domain name

VITE_API_URL=http://<EC2-PUBLIC-IP>:6000/api
